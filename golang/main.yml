---
- include: ./ubuntu.yml
  when: os == "linux"
  tags: [golang, install]

- name: Make sure golang workspace is present
  file:
    path: '{{ GOPATH }}'
    state: directory
  tags: [golang, setup]
