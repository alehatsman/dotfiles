---
# mac — macOS workstation.
#
# Apply:  provision apply ./mac.yml --ask-sudo-pass
# Plan:   provision plan  ./mac.yml
#
# -K prompts for the sudo password (needed for brew casks, system flags).

- vars_file: ./shared/variables.yml
- vars_file: ./machines/mac/vars.yml
- import: ./machines/mac/index.yml
