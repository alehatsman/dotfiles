# work_mac — NVIDIA work macOS workstation.
# Apply: provision apply ./work_mac.yml --ask-sudo-pass
# Plan: provision plan ./work_mac.yml
# -K prompts for the sudo password (needed for brew casks, system flags).
- vars_file: ./shared/variables.yml
- vars_file: ./machines/work_mac/vars.yml
- import: ./machines/work_mac/index.yml
