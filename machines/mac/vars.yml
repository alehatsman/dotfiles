---
# mac-specific variables.
is_wsl: false

# ─── mcsearch — semantic-search MCP helper ─────────────────────────────────
# Inherits mcsearch_role=client from shared/variables.yml. Prerequisite:
# `Host main_pc-wsl` alias in ~/.ssh/config; mcsearch-tunnel relies on it.
