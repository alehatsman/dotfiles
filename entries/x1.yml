---
# x1 — ThinkPad X1 Carbon, Arch Linux, Hyprland Wayland.
# Run with:
#   mooncake apply -c entries/x1.yml -v variables.yml -v vars/x1.yml -K

- import: ../platforms/arch/main.yml
  tags: [arch, system, packages]

- import: ../common.yml

- name: Install Hyprland desktop
  use:
    name: hyprland
  tags: [gui, hyprland]

- import: ../hyprland/main.yml
  tags: [gui, hyprland, config]
